#include <stdio.h>

int main() {
    int testvar;
    int deez = 1;
    int nuts;
    printf("Hello, World!\n");
    scanf("%d", &testvar);
    printf("%d", testvar);
    getchar();
    if(testvar > 9) {
        printf("please say this is like java");
    }
    else if(testvar > 8)
        printf("one liners???");
    else;
    for(nuts; deez; nuts++)
        printf("%d", nuts);
    return 0;
}
